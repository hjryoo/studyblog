[[plugins]]
package = "netlify-plugin-jekyll-cache"

[build]
# 수정됨: 반드시 'bundle install &&'가 앞에 있어야 합니다.
command = "bundle install && bundle exec jekyll build --incremental"
publish = "_site"

[build.environment]
RUBY_VERSION = "3.4.5"
JEKYLL_ENV = "production"
BUNDLE_JOBS = "4"
BUNDLE_RETRY = "3"
